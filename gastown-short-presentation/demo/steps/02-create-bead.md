# Create Work

A user request becomes a bead — the unit of work in Gas Town.
We'll create a real task for the conclave incident response platform.

```sh
bd create "Add liveness probe to the etherpad deployment"
```

Show the bead ID. This is now a trackable, auditable work item.

```sh
bd list
```
