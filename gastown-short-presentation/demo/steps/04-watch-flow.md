# Watch it Flow

Check the mail to see the agents communicating.

```sh
gt mail inbox
```

The refinery picks up the bead, does the work, and reports back.
The witness reviews automatically.

```sh
gt rig status conclave
```

Show the ledger — every completion is recorded.

```sh
bd log
```
