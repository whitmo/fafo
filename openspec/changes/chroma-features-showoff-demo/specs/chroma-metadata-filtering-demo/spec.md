## ADDED Requirements

### Requirement: Chroma Metadata Filtering Demo
The demo SHALL showcase filtering documents based on metadata during search operations in a ChromaDB collection.

#### Scenario: Search with Metadata Filter
- **WHEN** a search query is executed with a metadata filter
- **THEN** only documents matching both the query and the metadata filter SHALL be returned.
