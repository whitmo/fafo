## ADDED Requirements

### Requirement: Chroma Regex Search Demo
The demo SHALL showcase regex search capabilities within a ChromaDB collection.

#### Scenario: Successful Regex Search
- **WHEN** a regex query is executed against the collection
- **THEN** relevant documents matching the regex pattern SHALL be returned.
